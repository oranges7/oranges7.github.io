---
title: 单细胞下游分析
date: 2026-01-14
share: "true"
dir: content/post
image: images/Example-1.jpg
description:
tags:
  - 单细胞
categories: 单细胞
---
## 导入包和函数
```R
# ========== 0.导入包和函数==========

mycol22 <- c("#b0d45d","#356d67","#42465c","#5066a1",
             "#76afda","#ffc556",
             "#f06152","#eb998b","#a14462","#cca69c",
             "#9e6c69","#7d4444","#562e3c","#a14462","#eb998b",
             "#fddbc8","#356d67","#4c9568",
             "#7fb961","#b0d45d","#ffe788",
             "#f06152","#7d4444","#9e6c69","#cca69c",
             "#5066a1","#76afda","#e8743c","#2873B3","#2EBEBE","#b20000","#74B346","#167153","#7D4444","#A14462","#8264CC",
             "#BD6263","#8EA325","#A9D179","#84CAC0","#F5AE6B","#BCB8D3","#4387B5",
             "#E64B35B2", "#4DBBD5B2" ,"#00A087B2", "#3C5488B2", "#F39B7FB2",
             "#8491B4B2", "#91D1C2B2", "#DC0000B2", "#7E6148B2",
             "#999999", "#E69F00", "#56B4E9", "#009E73", "#F0E442", "#0072B2",
             "#D55E00", "#CC79A7",
             "#004529","#088247","#7CC767",
             "#223D6C","#5D90BA","#9ecae1",
             "#8c510a","#bf812d","#D8D155",
             "#b30000","#d73027","#fc9272",
             "#7A142C","#E0367A","#df65b0",
             "#4a1486","#6a51a3","#9e9ac8",
             "#252525","#737373","#bdbdbd")
mycol22 <- unique(mycol22)
save_plots <- function(
    filename,      # 基础文件名(不含扩展名)
    plot,          # 要保存的图形对象(ggplot或grid对象)
    path = ".",    # 保存路径,默认当前目录
    width = 7.2,   # 宽度(英寸)
    height = 4.8,  # 高度(英寸)
    dpi = 600,     # PNG分辨率
    formats = c("pdf", "png")  # 支持保存的格式
) {
  library(ggplot2)
  library(grid)
  
  # 创建目录(如果不存在)
  if (!dir.exists(path)) {
    dir.create(path, recursive = TRUE)
  }
  
  # 判断是否为ggplot对象
  is_ggplot <- inherits(plot, "ggplot")
  
  # 遍历所有需要保存的格式
  for (fmt in formats) {
    full_path <- file.path(path, paste0(filename, ".", fmt))
    
    if (is_ggplot) {
      # 如果是ggplot对象,使用ggsave
      switch(fmt,
             "pdf" = ggsave(full_path, plot, width = width, height = height),
             "png" = ggsave(full_path, plot, width = width, height = height, dpi = dpi, bg = "white"),
             "tiff" = ggsave(full_path, plot, width = width, height = height, dpi = dpi, bg = "white"),
             warning(paste("Unsupported format:", fmt))
      )
    } else {
      # 如果是grid对象(如pheatmap),使用传统图形设备
      switch(fmt,
             "pdf" = {
               pdf(full_path, width = width, height = height)
               print(plot)
               dev.off()
             },
             "png" = {
               png(full_path, width = width * dpi, height = height * dpi, res = dpi, bg = "white")
               print(plot)
               dev.off()
             },
             "tiff" = {
               tiff(full_path, width = width * dpi, height = height * dpi, res = dpi, bg = "white")
               print(plot)
               dev.off()
             },
             warning(paste("Unsupported format:", fmt))
      )
    }
    
    message(paste("Saved:", full_path))
  }
  
  invisible(NULL)
}
set_repel_family <- function(p, family = "Liberation Sans",
                             also_set_theme_text = TRUE,
                             verbose = FALSE) {
  stopifnot(inherits(p, "ggplot"))
  geom1 <- vapply(p$layers, \(x) class(x$geom)[1], character(1))
  idx <- which(geom1 %in% c("GeomTextRepel", "GeomLabelRepel"))
  if (!length(idx)) {
    if (verbose) message("No GeomTextRepel/GeomLabelRepel layers found.")
    return(if (also_set_theme_text) p + ggplot2::theme(text = ggplot2::element_text(family = family)) else p)
  }
  for (i in idx) {
    p$layers[[i]]$aes_params$family  <- family
    p$layers[[i]]$geom_params$family <- family
  }
  if (also_set_theme_text) {
    p <- p + ggplot2::theme(text = ggplot2::element_text(family = family))
  }
  if (verbose) message("Updated repel layer(s): ", paste(idx, collapse = ", "))
  p
}
library(Seurat)
library(dplyr)
library(ggplot2)
library(tidydr)
library(patchwork)
library(scop)
options(reticulate.conda_binary = "/BioData/00.Software/12.anaconda/anaconda3/bin/conda")
```
## 数据读取与质控
数据读取有多种类型，需要根据实际情况进行读取
```R
# ========== 1.数据导入==========
raw_counts <- Read10X(data.dir = "/xxx/filter_matrix/")
obj1 <- CreateSeuratObject(
  counts = raw_counts,
  project = "NC1",
  min.cells = 3
)
raw_counts <- Read10X(data.dir = "/xxx/filter_matrix/")
obj2 <- CreateSeuratObject(
  counts = raw_counts,
  project = "NC2",
  min.cells = 3
)
raw_counts <- Read10X(data.dir = "/xxx/filter_matrix/")
obj3 <- CreateSeuratObject(
  counts = raw_counts,
  project = "NC3",
  min.cells = 3
)
merged <- merge(obj1, 
                y = c(obj2,obj3),
                add.cell.ids = c("NC1", "NC2", "NC3"),
                project = "merged")


table(merged$orig.ident)
merged$Sample <- merged$orig.ident
merged <- JoinLayers(merged)

# ========== 2.质控==========
species <- "Mouse"

if (species == "Mouse") {
  merged[["percent.mt"]] <- PercentageFeatureSet(merged, pattern = "^mt-")
} else if (species == "Human") {
  merged[["percent.mt"]] <- PercentageFeatureSet(merged, pattern = "^MT-")
} else {
  stop("Unsupported species. Please specify 'Mouse' or 'Human'.")
}
v1 <- VlnPlot(merged,
              features = c("nFeature_RNA", "nCount_RNA", "percent.mt"),
              ncol = 3,group.by = "orig.ident",cols = mycol22,
              pt.size = 0)&
  theme(axis.title.x = element_blank(),
        text = element_text(family = "serif"))
v1
save_plots(filename="00.VlnPlot",
           plot = v1,
           width = 7.9,   # 宽度（英寸）
           height = 4.75,
           path = "result"
)
merged <- subset(
  merged,
  subset = nFeature_RNA > 200 &
    nFeature_RNA < 6000 &
    nCount_RNA > 600 &
    nCount_RNA < 40000 &
    percent.mt < 15
)
v2 <- VlnPlot(merged,
              features = c("nFeature_RNA", "nCount_RNA", "percent.mt"),
              ncol = 3,group.by = "orig.ident",cols = mycol22,
              pt.size = 0)&
  theme(axis.title.x = element_blank(),
        text = element_text(family = "serif"))
v2
save_plots(filename="00.VlnPlot_qc",
           plot = v2,
           width = 7.9,   # 宽度（英寸）
           height = 4.75,
           path = "result"
)
saveRDS(merged, file = "01.merged_qc.rds")
```
## 聚类
```R
merged <- NormalizeData(merged)
merged <- FindVariableFeatures(merged,
                               selection.method = "vst",
                               nfeatures = 3000)
#plan(sequential)
merged <- ScaleData(merged)
#merged <- ScaleData(merged, features = rownames(merged))
#plan(multisession, workers = 32)
merged <- RunPCA(merged,
                 npcs = 50,
                 verbose = FALSE)

#ElbowPlot(merged, ndims = 50,reduction = "pca")
e1 <- ElbowPlot(merged, ndims = 50,reduction = "pca")
e1
save_plots("01.ElbowPlot", e1,
           path = "result",
           width = 9.5,
           height = 6.2)
merged <- FindNeighbors(merged,
                        reduction = "pca",
                        dims = 1:30)

merged <- FindClusters(merged, resolution = 0.6)

merged <- RunUMAP(merged,
                  reduction = "pca", min.dist = 0.3,
                  dims = 1:30)
library(harmony)
merged <- RunHarmony(merged,
                     group.by.vars = "orig.ident", 
                     max_iter = 30)
e1 <- ElbowPlot(merged, ndims = 30,reduction = "harmony")
e1
dim_num <- 20
merged <- FindNeighbors(merged, 
                        reduction = "harmony",  # 使用harmony!
                        dims = 1:dim_num)
merged <- FindClusters(merged,resolution = 0.6)
merged <- RunUMAP(merged, 
                  reduction = "harmony",  
                  dims = 1:dim_num,
                  reduction.name = "umap_harmony")


p1<-CellDimPlot(
  srt = merged,
  group.by = c("seurat_clusters","Sample"),
  reduction = "umap_harmony",
  label = T,
  label_insitu =T,
  theme_use = "theme_blank"
)
p1

save_plots("02.umap_clusters", p1,
           path = "result",
           width = 7.8,
           height = 6.5)
```
## 注释
### 气泡图
```R
cell_markers <- list(
  `Classical_MSCs` = c("CD44", "ENG", "NT5E", "THY1", "CD105"),
  `Proliferative_MSC` = c("UBE2C", "TOP2A", "CENPF"),
  `Matrix/fibroblast-like_MSCs` = c("COL1A1", "COL5A1", "COL3A1"),
  `Biofunctional-type_MSC` = c("VEGF", "ANG", "SOD2", "SOD3"),
  `Lineage-Primed_MSCs` = c("SOX9", "BMP2", "MMP13", "RANK", "RUNX2"),
  `Dedifferentiated / fibrocartilage‑like chondrocytes` = c("ACAN", "COMP", "SOX9"),
  `Chondrocyte` = c("COL2A1", "PRG4", "ACAN", "COMP", "SOX9"),
  `Activated Monocyte` = c("NR4A1","PTGS2"),
  `Pericyte` = c("PDGFRB", "RGS5", "CSPG4", "ACTA2", "CD146"),
  `Macrophages` = c("PTPRC", "CD68", "LYZ", "CD45"),
  `FLS` = c("IL6", "FAP", "PRG4", "CLEC4C","TCF4")
)

genes_to_check <- unlist(cell_markers, use.names = FALSE)
gene_cell_type <- stack(cell_markers) %>% 
  dplyr::rename(gene = values, cell_type = ind)

dot_plot <- DotPlot(merged, features = unique(genes_to_check), 
                    group.by = 'seurat_clusters', assay = 'RNA')$data

dot_data <- dot_plot %>%
  left_join(gene_cell_type, by = c("features.plot" = "gene")) %>%
  arrange(cell_type)  

p3 <- ggplot(dot_data, aes(x = id, y = features.plot)) +
  geom_point(aes(size = pct.exp, color = avg.exp.scaled)) +
  scale_color_gradient2(low = "blue", mid = "white", high = "red") +
  scale_size(range = c(0, 6)) +
  theme_classic() +
  labs(x = "Cluster", y = "Gene", color = "Avg Exp", size = "Pct Exp") +
  theme(
    axis.text.x = element_text(),
    strip.background = element_blank(),
    panel.border = element_rect(color = "black", linewidth = 1, fill = NA),
    strip.text.y.right = element_text(angle = 360, hjust = 0.5, vjust = 0.5, face = "bold"),
  ) +
  facet_grid(cell_type ~ ., scales = "free_y", space = "free_y")  

print(p3)

save_plots(filename="04.dot_marker",
           plot = p3,
           width = 9,   
           height = 8,
           path = "result")


merged@meta.data <- merged@meta.data %>%
  mutate(
    Celltype = case_when(
      seurat_clusters %in% c("12") ~ "Proliferative MSC",
      seurat_clusters %in% c("10") ~ "Macrophage",
      seurat_clusters %in% c("15") ~ "Osteoclast",
      seurat_clusters %in% c("13","6","9","17","11") ~ "Immunomodulatory MSC",
      seurat_clusters %in% c("16") ~ "Pericyte",
      seurat_clusters %in% c("5","3","8","7","4") ~ "Chondrogenic differentiation of MSC",
      seurat_clusters %in% c("0","1","2","14") ~ "Matrix/fibroblast-like MSC"
    )
  )
 # 保持颜色映射
celltypes <- unique(ccRCC$Celltype)
ccRCC$Celltype <- factor(ccRCC$Celltype, levels = celltypes)
color_mapping <- setNames(
  mycol22[seq_along(celltypes)],   # 第一个参数：object（颜色向量）
  celltypes                    # 第二个参数：nm（名字）
)
```
### 自动注释
使用Deepcellseek进行自动注释，对细胞类型初步注释的时候效果还是可以的
```R
library(DeepCellSeek)
markers_df <- FindAllMarkers(object = merged,
                              only.pos = TRUE,
                              min.pct = 0.5,
                              logfc.threshold = 0.3)

Sys.setenv(USE_DEEPCELLSEEK_API = "TRUE")

annotations <- deepcellseek_celltype(
  input = markers_df,
  tissuename = "cartilage",  # Providing tissue context is important
  species = "Human",
  #species = "Mouse",
  topgenenumber =10,
  model = "gpt-5"  # Choose any supported model
)

merged@meta.data$LLM_Annotation <- as.factor(annotations[as.character(Idents(merged))])
test1 <- CellDimPlot(
  srt = merged,
  group.by = c("LLM_Annotation"),
  reduction = "umap_harmony",
  label = T,
  label_insitu =T,
  theme_use = "theme_blank"
)
test1
save_plots(filename="test1",
           plot = test1,
           width = 15, 
           height = 6,
           path = "result")


```
## 差异与富集分析
```R
#====================差异富集分析=======================

library(Seurat)
library(clusterProfiler)
library(ggplot2)
library(dplyr)

# ========== 用户设置区域 ==========
species <- "Rat"           # 选项: "Human" 或 "Mouse"


# ========== 自动配置数据库 ==========
if (species == "Human") {
  library(org.Hs.eg.db)
  org_db <- org.Hs.eg.db
  kegg_organism <- "hsa"
} else if (species %in% c("Mouse", "Mus", "Mus musculus")) {
  library(org.Mm.eg.db)
  org_db <- org.Mm.eg.db
  kegg_organism <- "mmu"
} else if (species %in% c("Rat", "Rattus", "Rattus norvegicus")) {
  library(org.Rn.eg.db)
  org_db <- org.Rn.eg.db
  kegg_organism <- "rno"
  message("使用大鼠数据库: org.Rn.eg.db, KEGG: rno")
} else {
  stop("species 必须是 'Human', 'Mouse' 或 'Rat'")
}

message(paste0("实验组 (ident.1): ", group_exp))
message(paste0("对照组 (ident.2): ", group_ctrl))

# 创建输出目录
if (!dir.exists("result/Enrichment")) {
  dir.create("result/Enrichment", recursive = TRUE)
}

# 要分析的细胞类型列表
cell_types_to_analyze <- unique(merged$Celltype)

# 初始化序号计数器（从00开始）
counter <- 0

# 遍历每个细胞类型
for (cell_type in cell_types_to_analyze) {
  
  message(paste0("\n========== 分析细胞类型: ", cell_type, " =========="))
  
  # 生成序号前缀（00, 01, 02...）
  id_str <- sprintf("%02d", counter)
  
  # 清理细胞类型名称用于文件名（替换特殊字符为下划线）
  cleaned_ct <- gsub("[^a-zA-Z0-9]", "_", cell_type)
  
  # 1. 提取特定细胞类型子集
  sub_obj <- subset(merged, subset = Celltype == cell_type)
  
  # 检查分组情况
  cell_counts <- table(sub_obj$Group)
  message("细胞数量: ", paste(names(cell_counts), cell_counts, sep = ":", collapse = ", "))
  
  if (length(cell_counts) < 2 || any(cell_counts < 3)) {
    warning(paste0(cell_type, ": 某组细胞数量不足，跳过"))
    counter <- counter + 1
    next
  }
  
  # 2. 多组差异分析（每组 vs 其他所有组）
  message("运行多组差异分析 (FindAllMarkers)...")
  de_genes_all <- FindAllMarkers(
    sub_obj,
    group.by = "Group",     # 明确指定分组变量
    min.pct = 0.4,
    logfc.threshold = 0.5,
    only.pos = FALSE,       # 同时获取上调和下调基因（后续会分开）
    test.use = "wilcox"     # 可根据需要调整
  )
  
  # 查看结果结构
  # head(de_genes_all)
  # 列包括: gene, avg_log2FC, pct.1, pct.2, p_val_adj, group (该基因在哪个组中显著)
  
  if (nrow(de_genes_all) == 0) {
    warning(paste0(cell_type, ": 未找到任何差异基因"))
    counter <- counter + 1
    next
  }
  
  message(paste0("总差异基因数: ", nrow(de_genes_all)))
  
  colnames(de_genes_all)[colnames(de_genes_all) == "cluster"] <- "group"
  
  # 3. 为每组分别准备基因列表
  DEG_list <- list()
  
  for (grp in unique(de_genes_all$group)) {
    grp_genes <- de_genes_all %>%
      filter(group == grp) %>%
      arrange(desc(avg_log2FC)) %>%
      pull(gene)
    
    if (length(grp_genes) > 0) {
      DEG_list[[grp]] <- grp_genes
      message(paste0(grp, " 高表达基因数: ", length(grp_genes)))
    }
  }
  
  if (length(DEG_list) == 0) {
    warning(paste0(cell_type, ": 无有效基因列表"))
    counter <- counter + 1
    next
  }
  
  # 4. ID转换
  message("转换ENTREZ ID...")
  DEG_list_entrez <- list()
  
  for (grp in names(DEG_list)) {
    genes_symbol <- DEG_list[[grp]]
    
    # 测试 bitr 是否能正常工作
    test_genes <- head(genes_symbol, 100)  # 测试前100个
    test_bitr <- tryCatch(
      bitr(test_genes, fromType = "SYMBOL", toType = "ENTREZID", OrgDb = org_db),
      error = function(e) {
        message(paste0("组 ", grp, " ID转换测试失败: ", e$message))
        return(NULL)
      }
    )
    
    if (is.null(test_bitr) || nrow(test_bitr) == 0) {
      message(paste0(grp, " ID转换测试失败，跳过"))
      next
    }
    
    # 正式转换所有基因
    entrez_df <- tryCatch(
      bitr(genes_symbol, fromType = "SYMBOL", toType = "ENTREZID", OrgDb = org_db),
      error = function(e) {
        message(paste0("组 ", grp, " ID转换失败: ", e$message))
        return(NULL)
      }
    )
    
    if (is.null(entrez_df) || nrow(entrez_df) == 0) {
      message(paste0(grp, " ID转换无结果，跳过"))
      next
    }
    
    entrez_ids <- entrez_df$ENTREZID
    entrez_ids <- unique(entrez_ids[!is.na(entrez_ids)])
    
    if (length(entrez_ids) >= 3) {  # 降低阈值
      DEG_list_entrez[[grp]] <- entrez_ids
      message(paste0(grp, " 成功映射: ", length(entrez_ids), " 个基因"))
    } else {
      message(paste0(grp, " 映射基因数过少 (", length(entrez_ids), ")，跳过富集"))
    }
  }
  
  if (length(DEG_list_entrez) == 0) {
    warning(paste0(cell_type, ": 无足够基因进行富集分析"))
    counter <- counter + 1
    next
  }
  
  # ========== 5. KEGG富集（每组单独） ==========
  if (length(DEG_list_entrez) >= 1) {
    comp_kegg <- compareCluster(
      geneClusters = DEG_list_entrez,
      fun = "enrichKEGG",
      organism = kegg_organism,
      pAdjustMethod = "BH",
      pvalueCutoff = 0.05,
      qvalueCutoff = 0.2
    )
    
    if (!is.null(comp_kegg) && nrow(as.data.frame(comp_kegg)) > 0) {
      comp_kegg <- setReadable(comp_kegg, OrgDb = org_db, keyType = "ENTREZID")
      kegg_df_full <- as.data.frame(comp_kegg)
      
      # 去除KEGG通路名称中的物种后缀
      suffix_to_remove <- if (species == "Human") {
        " - Homo sapiens \\(human\\)"
      } else if (species == "Mouse") {
        " - Mus musculus \\(house mouse\\)"
      } else if (species == "Rat") {
        " - Rattus norvegicus \\(rat\\)"
      } else {
        ""  # 其他物种可能没有标准后缀
      }
      kegg_df_full$Description <- sub(suffix_to_remove, "", kegg_df_full$Description)
      
      # 筛选每组前10条
      kegg_df <- kegg_df_full %>%
        group_by(Cluster) %>%
        slice_min(p.adjust, n = 10) %>%
        ungroup()
      
      # 可视化
      p_kegg <- ggplot(kegg_df, 
                       aes(x = Cluster,
                           y = reorder(Description, log10(p.adjust)),
                           color = -log10(p.adjust),
                           size = Count)) +
        geom_point(alpha = 0.85, stroke = 0.5) +
        scale_color_gradientn(
          colors = c("#FDDBC7", "#F4A582", "#D6604D", "#B2182B"),
          name = "-log10(Padj)",
          guide = guide_colorbar(barheight = unit(4, "cm"), barwidth = unit(0.8, "cm"),
                                 frame.colour = "black", ticks.colour = "black")
        ) +
        scale_size_continuous(name = "Gene Count", range = c(3, 10), 
                              breaks = c(5, 10, 20, 50, 100)) +
        labs(
          x = "Group",
          y = "Pathway",
        ) +
        theme_minimal(base_size = 12) +
        theme(
          plot.title = element_text(hjust = 0.5, face = "bold", size = 14),
          plot.subtitle = element_text(hjust = 0.5, size = 11, color = "gray40"),
          axis.text.x = element_text(angle = 45, hjust = 1, size = 12),
          axis.text.y = element_text(size = 12),
          axis.title.x = element_text(face = "bold", size = 13, margin = margin(t = 10)),
          axis.title.y = element_text(face = "bold", size = 13, margin = margin(r = 10)),
          legend.position = "right",
          panel.grid.minor = element_blank(),
          panel.grid.major = element_line(color = "gray90", linewidth = 0.3),
          panel.border = element_rect(color = "gray40", fill = NA, linewidth = 0.5),
          text = element_text(family = "Liberation Sans")
        )
      
      w <- length(unique(kegg_df$Cluster)) * 0.7 + 6.5
      h <- length(unique(kegg_df$Description)) * 0.36 + 1
      
      save_plots(
        filename = paste0(id_str, ".KEGG_", cleaned_ct),
        plot = p_kegg,
        path = "result/Enrichment",
        width = w, height = h
      )
      
      write.csv(kegg_df_full, 
                file.path("result/Enrichment", paste0(id_str, ".KEGG_", cleaned_ct, ".csv")), 
                row.names = FALSE)
      message("KEGG完成: ", nrow(kegg_df), " 通路")
    }
  }
  
  # ========== 6. GO富集 (BP/CC/MF) ==========
  for (ont in c("BP", "CC", "MF")) {
    comp_go <- compareCluster(
      geneClusters = DEG_list_entrez,
      fun = "enrichGO",
      OrgDb = org_db,
      ont = ont,
      pAdjustMethod = "BH",
      pvalueCutoff = 0.05,
      qvalueCutoff = 0.2,
      readable = TRUE
    )
    
    if (!is.null(comp_go) && nrow(as.data.frame(comp_go)) > 0) {
      go_df <- as.data.frame(comp_go)
      go_df <- go_df %>% 
        group_by(Cluster) %>% 
        slice_min(p.adjust, n = 10) %>% 
        ungroup()
      
      p_go <- ggplot(go_df, 
                     aes(x = Cluster,
                         y = reorder(Description, log10(p.adjust)),
                         color = -log10(p.adjust),
                         size = Count)) +
        geom_point(alpha = 0.85, stroke = 0.5) +
        scale_color_gradientn(
          colors = c("#FDDBC7", "#F4A582", "#D6604D", "#B2182B"),
          name = "-log10(Padj)",
          guide = guide_colorbar(barheight = unit(4, "cm"), barwidth = unit(0.8, "cm"),
                                 frame.colour = "black", ticks.colour = "black")
        ) +
        scale_size_continuous(name = "Gene Count", range = c(3, 10), 
                              breaks = c(5, 10, 20, 50, 100)) +
        labs(
          x = "Group",
          y = "Pathway",
        ) +
        theme_minimal(base_size = 12) +
        theme(
          plot.title = element_text(hjust = 0.5, face = "bold", size = 14),
          plot.subtitle = element_text(hjust = 0.5, size = 11, color = "gray40"),
          axis.text.x = element_text(angle = 45, hjust = 1, size = 12),
          axis.text.y = element_text(size = 12),
          axis.title.x = element_text(face = "bold", size = 13, margin = margin(t = 10)),
          axis.title.y = element_text(face = "bold", size = 13, margin = margin(r = 10)),
          legend.position = "right",
          panel.grid.minor = element_blank(),
          panel.grid.major = element_line(color = "gray90", linewidth = 0.3),
          panel.border = element_rect(color = "gray40", fill = NA, linewidth = 0.5),
          text = element_text(family = "Liberation Sans")
        )
      
      w <- length(unique(go_df$Cluster)) * 0.7 + 5.5
      h <- length(unique(go_df$Description)) * 0.36 + 1
      
      save_plots(
        filename = paste0(id_str, ".GO_", ont, "_", cleaned_ct),
        plot = p_go,
        path = "result/Enrichment",
        width = w, height = h
      )
      
      write.csv(as.data.frame(comp_go), 
                file.path("result/Enrichment", paste0(id_str, ".GO_", ont, "_", cleaned_ct, ".csv")), 
                row.names = FALSE)
      message("GO-", ont, "完成: ", nrow(go_df), " 条目")
    }
  }
  
  counter <- counter + 1
  rm(sub_obj, de_genes_all, DEG_list, DEG_list_entrez)
}

message(paste0("\n完成! 共", counter, "个细胞类型 | 物种:", species, 
               " | 分组:", group_exp, " vs ", group_ctrl))

```
### 重新绘制富集分析结果
如果对绘制结果不满意，又不想重新运行，可以直接使用下面的代码对气泡图重新绘制
```R


# ========== 重新绘制富集分析结果 ==========
# 输出目录（确保与之前分析时一致）
output_dir <- "result/Enrichment"

# 获取所有CSV文件列表
csv_files <- list.files(output_dir, pattern = "\\.csv$", full.names = TRUE)


for (csv_file in csv_files) {
  
  # 获取文件名（不带路径）
  file_name <- basename(csv_file)
  
  # 移除.csv后缀，得到基础名称（用于图片命名）
  base_name <- tools::file_path_sans_ext(file_name)
  
  # 跳过DEG文件（差异基因表不画图）
  if (str_detect(base_name, "^\\d+\\.DEG_")) {
    next
  }
  
  message(paste0("\n处理文件: ", file_name))
  
  # 读取CSV数据
  df <- read.csv(csv_file, stringsAsFactors = FALSE)
  
  # 检查必要列是否存在
  required_cols <- c("Cluster", "Description", "p.adjust", "Count")
  if (!all(required_cols %in% colnames(df))) {
    warning(paste0("文件缺少必要列，跳过: ", file_name))
    next
  }
  
  # 提取细胞类型（从文件名中最后一个下划线后提取）
  # 例如: "00.KEGG_Monocyte" -> "Monocyte"
  #       "00.GO_BP_T_Cell"   -> "T_Cell"
  cell_type <- str_extract(base_name, "(?<=_)[^_]+$")
  if (is.na(cell_type)) {
    cell_type <- base_name
  }
  
  # 提取分析类型（KEGG 或 GO-BP/CC/MF）
  # 例如: "00.KEGG_Monocyte" -> "KEGG"
  #       "00.GO_BP_Monocyte" -> "GO-BP"
  analysis_type <- ifelse(str_detect(base_name, "KEGG"), "KEGG",
                          ifelse(str_detect(base_name, "GO_BP"), "GO-BP",
                                 ifelse(str_detect(base_name, "GO_CC"), "GO-CC", "GO-MF")))
  
  df <- df %>%
    group_by(Cluster) %>%
    slice_min(p.adjust, n = 10) %>%  # 修改点：每组取前10
    ungroup()
  p <- ggplot(df, 
              aes(x = Cluster,
                  y = reorder(Description, log10(p.adjust)),
                  color = -log10(p.adjust),
                  size = Count)) +
    geom_point(alpha = 0.85, stroke = 0.5) +
    scale_color_gradientn(
      colors = c("#FDDBC7", "#F4A582", "#D6604D", "#B2182B"),
      name = "-log10(Padj)",
      guide = guide_colorbar(barheight = unit(4, "cm"), barwidth = unit(0.8, "cm"),
                             frame.colour = "black", ticks.colour = "black")
    ) +
    scale_size_continuous(name = "Gene Count", range = c(3, 10), 
                          breaks = c(5, 10, 20, 50, 100)) +
    labs(
      title = paste0(analysis_type),
      x = "Group",
      y = ifelse(str_detect(analysis_type, "KEGG"), "Pathway", "GO Term")
    ) +
    theme_minimal(base_size = 12) +
    theme(
      plot.title = element_text(hjust = 0.5, face = "bold", size = 14),
      plot.subtitle = element_text(hjust = 0.5, size = 11, color = "gray40"),
      axis.text.x = element_text(angle = 45, hjust = 1, size = 12),
      axis.text.y = element_text(size = 12),
      axis.title.x = element_text(face = "bold", size = 13, margin = margin(t = 10)),
      axis.title.y = element_text(face = "bold", size = 13, margin = margin(r = 10)),
      legend.position = "right",
      panel.grid.minor = element_blank(),
      panel.grid.major = element_line(color = "gray90", linewidth = 0.3),
      panel.border = element_rect(color = "gray40", fill = NA, linewidth = 0.5),
      text = element_text(family = "Liberation Sans")
    )
  
  # 计算合适的图片尺寸
  w <- length(unique(df$Cluster)) * 0.7 + 6.5
  h <- length(unique(df$Description)) * 0.36 + 1
  
  
  save_plots(filename = base_name,
             plot = p,
             path = "result/Enrichment",
             width = w, height = h)
  
}

```




## 细胞通讯
```R
#====================细胞通讯=========================
library(CellChat)
run_cellchat <- function(seurat_obj, group_name, celltype_col = "celltype", organism = "human") {
  
  cat(paste0(">>> Start processing: ", group_name, " <<<\n"))
  
  # 1. 处理rat物种：将基因名映射为小鼠基因
  if (organism == "rat") {
    cat(">>> Converting rat genes to mouse genes <<<\n")
    
    # 基因转换：rat -> mouse
    res <- GeneConvert(
      geneID = rownames(seurat_obj),
      geneID_from_IDtype = "symbol",
      geneID_to_IDtype = "symbol",
      species_from = "Rattus_norvegicus",
      species_to = "Mus_musculus"
    )
    
    # 使用collapse版本
    gene_mapping <- res$geneID_collapse
    
    # 检查列名
    cat(">>> Column names in gene_mapping: <<<\n")
    print(colnames(gene_mapping))
    print(head(gene_mapping))
    
    # 正确的列名应该是 'symbol' (目标基因名)
    # rownames 是原始基因名（rat）
    # symbol 列是转换后的基因名（mouse）
    
    # 过滤掉NA或空值
    valid_mapping <- gene_mapping[!is.na(gene_mapping$symbol) & 
                                    gene_mapping$symbol != "" &
                                    nchar(as.character(gene_mapping$symbol)) > 0, ]
    
    cat(paste0(">>> Valid mappings: ", nrow(valid_mapping), " <<<\n"))
    
    if (nrow(valid_mapping) == 0) {
      stop("No valid gene mappings found!")
    }
    
    # 提取旧基因名（rat）和新基因名（mouse）
    old_names <- rownames(valid_mapping)  # rat基因名
    new_names_vec <- as.character(valid_mapping$symbol)  # mouse基因名
    
    cat(paste0(">>> old_names length: ", length(old_names), " <<<\n"))
    cat(paste0(">>> new_names_vec length: ", length(new_names_vec), " <<<\n"))
    cat(">>> First few mappings: <<<\n")
    print(head(data.frame(rat = old_names, mouse = new_names_vec)))
    
    # 创建命名向量：新名字 = 旧名字
    rename_vector <- setNames(new_names_vec, old_names)
    
    # 过滤Seurat对象中存在的基因
    genes_to_keep <- intersect(rownames(seurat_obj), names(rename_vector))
    
    if (length(genes_to_keep) == 0) {
      stop("No genes could be mapped from rat to mouse!")
    }
    
    cat(paste0(">>> Genes to keep: ", length(genes_to_keep), " <<<\n"))
    
    # 只保留需要的基因
    seurat_obj <- subset(seurat_obj, features = genes_to_keep)
    rename_vector <- rename_vector[genes_to_keep]
    
    # 使用RenameFeatrues重命名基因
    seurat_obj <- RenameFeatures(seurat_obj, newnames = rename_vector)
    
    cat(paste0(">>> Mapped ", length(genes_to_keep), " genes to mouse orthologs <<<\n"))
    cat(paste0(">>> Unmapped genes: ", length(res$geneID_unmapped), " <<<\n"))
    
    # rat使用mouse数据库
    organism <- "mouse"
  }
  # --- 修改结束 ---
  # 1. 设置数据库
  if (organism == "human") {
    CellChatDB <- CellChatDB.human
  } else {
    CellChatDB <- CellChatDB.mouse
  }
  
  # 2. 创建 CellChat 对象
  cellchat <- createCellChat(seurat_obj, 
                             meta = seurat_obj@meta.data, 
                             group.by = celltype_col)
  cellchat@DB <- CellChatDB
  
  # 3. 预处理流程
  cellchat <- subsetData(cellchat)
  cellchat <- identifyOverExpressedGenes(cellchat)
  cellchat <- identifyOverExpressedInteractions(cellchat)
  
  # 4. 计算通讯概率
  cellchat <- computeCommunProb(cellchat, population.size = TRUE)
  cellchat <- filterCommunication(cellchat) 
  
  # 5. 通路级计算与聚合
  cellchat <- computeCommunProbPathway(cellchat)
  cellchat <- aggregateNet(cellchat)
  
  # 6. 保存结果 (保存为 cco.PD.rds 格式)
  file_name <- paste0("cco.", group_name, ".rds")
  saveRDS(cellchat, file = file_name)
  cat(paste0(">>> Saved to: ", file_name, " <<<\n\n"))
  
  # 7. 返回对象供赋值使用
  return(cellchat)
}
```



## 拟时序分析
拟时序分析选择Slingshot，原因嘛，就是快
```R
#====================拟时序分析=========================
merged <- RunSlingshot(
  merged,
  approx_points = 150,
  group.by = "Celltype",
  reduction = "umap_harmony"
)
p10 <- CellDimPlot(
  sub_obj,
  group.by = "Celltype",
  reduction = "umap_harmony",
  lineages = paste0("Lineage", 1),
  lineages_span = 0.3,
  lineages_trim = c(0.05, 0.95)
)
save_plots("15.trajectory_Lineage", p8,
           path = "result",
           width = 10,
           height = 5)
merged <- RunCytoTRACE(
  merged,
  cores = 24,
  species = "mouse"
)
p11 <- CytoTRACEPlot(
  merged,
  reduction = "umap_harmony",
  group.by = "Celltype",
  combine = FALSE
)
p11$Score + p11$Potency
theme_set(theme(text = element_text(family = "Liberation Sans")))
save_plots("11.CytoTRACE_sp", 
           p11$Score + p11$Potency,
           path = "result",
           width = 12.6,
           height = 6.5)
p11$Boxplot <- p11$Boxplot +
  scale_x_discrete(guide = ggplot2::guide_axis(angle = 45))


save_plots("11.CytoTRACE_box", 
           p11$Boxplot,
           path = "result",
           width = 8.4,
           height = 6.5)
```

## 转录因子分析
在R中先导出count_mat.loom，使用sh脚本来计算出auc_mtx.loom，导入回R进行可视化


```R
#====================转录因子分析=========================

library(Seurat)
library(SCopeLoomR)
library(AUCell)
library(SCENIC)
library(dplyr)
library(KernSmooth)
library(RColorBrewer)
library(plotly)
library(BiocParallel)
library(grid)
library(ComplexHeatmap)
library(data.table)
#library(scRNAseq)
library(patchwork)
library(ggplot2) 
library(stringr)
library(circlize)
auto_plot_dim_for_rss <- function(ggobj) {
  p <- ggobj
  data <- p$data
  mapping <- p$mapping
  
  # 提取 x 和 y 变量名
  x_var <- rlang::as_name(mapping$x)
  y_var <- rlang::as_name(mapping$y)
  
  # 获取水平数（因是因子，直接取 nlevels；若非因子则用 unique）
  n_x <- if (x_var %in% names(data)) length(unique(data[[x_var]])) else 1
  n_y <- if (y_var %in% names(data)) length(unique(data[[y_var]])) else 1
  
  # x 标签长度（用于微调宽度）
  x_labels <- unique(data[[x_var]])
  max_x_len <- if (is.character(x_labels) || is.factor(x_labels)) {
    max(nchar(as.character(x_labels)), na.rm = TRUE)
  } else 0
  
  # 基础尺寸
  width  <- n_x * 0.3
  height <- n_y * 0.22
  
  
  
  cat("自动设定尺寸：宽 =", round(width,1), "英寸，高 =", round(height,1), "英寸\n")
  return(list(width = width, height = height))
}
build_loom(file.name = "count_mat.loom",
           # 使用 GetAssayData 提取 counts 层
           dgem = GetAssayData(merged, slot = "counts"), 
           title = "mergedloom")
loom <- open_loom('count_mat.loom', mode="r+")
close_loom(loom)




loom <- open_loom('auc_mtx.loom', mode="r+")


# 1. 从loom文件中提取数据
# 提取regulons活性矩阵
regulonAUC <- get_regulons_AUC(loom,column.attr.name='RegulonsAUC')
regulonAUC[1:5, 1:5]  # 查看前几行几列

# 提取regulons信息
regulons <- get_regulons(loom, column.attr.name="Regulons")
regulonAucThresholds <- get_regulon_thresholds(loom)
tail(regulonAucThresholds[order(as.numeric(names(regulonAucThresholds)))])
embeddings <- get_embeddings(loom)
close_loom(loom)

#####################


sub_regulonAUC <- regulonAUC[,match(colnames(merged),colnames(regulonAUC))]

merged$celltype.group <- paste(merged$Group,merged$Celltype, sep = "-")
cellTypes <- data.frame(row.names = colnames(merged),
                        celltype = merged$celltype.group)
selectedResolution <- "celltype" 


##################################RSS计算
rss <- calcRSS(AUC=getAUC(sub_regulonAUC), cellAnnotation=cellTypes[colnames(sub_regulonAUC), selectedResolution]) 
rss=na.omit(rss) 
rssPlot <- plotRSS(rss)
plotly::ggplotly(rssPlot$plot)
dims <- auto_plot_dim_for_rss(rssPlot$plot)
p12 <- rssPlot$plot + theme(axis.text.x = element_text(angle = 45, hjust = 1))

save_plots("12.RSS_Scores", p13, path = "result", width = dims$width, height = dims$height)

###################################

cellsPerGroup <- split(rownames(cellTypes),
                       cellTypes[,selectedResolution])
sub_regulonAUC <- sub_regulonAUC[onlyNonDuplicatedExtended(rownames(sub_regulonAUC)),]
dim(sub_regulonAUC)
regulonActivity_byGroup <- sapply(cellsPerGroup,
                                  function
                                  (cells)
                                    rowMeans(getAUC(sub_regulonAUC)[,cells]))

regulonActivity_byGroup_Scaled <- t(scale(t(regulonActivity_byGroup),
                                          center = 
                                            T, scale=T
))

dim(regulonActivity_byGroup_Scaled)
regulonActivity_byGroup_Scaled=regulonActivity_byGroup_Scaled[]
regulonActivity_byGroup_Scaled=na.omit(regulonActivity_byGroup_Scaled)
regulonActivity_byGroup_Scaled <- regulonActivity_byGroup_Scaled[complete.cases(regulonActivity_byGroup_Scaled), ]
regulonActivity_byGroup_Scaled <- regulonActivity_byGroup_Scaled[!apply(is.infinite(regulonActivity_byGroup_Scaled), 1, any), ]
regulon_avg_activity <- rowMeans(abs(regulonActivity_byGroup_Scaled))
top_regulons <- names(head(sort(regulon_avg_activity, decreasing = TRUE), 100))
library(pheatmap)
p13<-pheatmap(regulonActivity_byGroup_Scaled[top_regulons,], fontfamily = "Liberation Sans",
              angle_col = 90)
save_plots("13.Regulons_Activity_Heatmap", 
           p13, 
           path = "result", width = 7, height = 7.6)
```

使用pyscenic，需要先安装pyscenic，根据官方文档下载所需数据库文件，在下方替换为你的路径
```sh
# run_pyscenic_csv.sh

INPUT="count_mat.loom"
THREADS=32
SPECIES=$1
log=$2

abs_log=$(realpath -m "$log")
#SPECIES 1 为human 2为mouse
# 记录程序开始时间
start_time=$(date +"%Y-%m-%d %H:%M:%S")
start_timestamp=$(date +%s)

echo "程序开始时间: $start_time"
echo "运行命令: pyscenic"
echo "======================================"


source /BioData/00.Software/12.anaconda/anaconda3/etc/profile.d/conda.sh

conda activate pyscenic

# 设置 PySCENIC 变量
if [ "$SPECIES" == "1" ]; then
    echo "正在为 Human 设置 PySCENIC 路径..."
    TFS="/BioData/08.ref/pysenic/allTFS_hg38.txt"
    DB_DIR="/BioData/08.ref/pyscenic/human/hg38_10kbp_up_10kbp_down_full_tx_v10_clust.genes_vs_motifs.rankings.feather"
    MOTIFS="/BioData/08.ref/pyscenic/motifs-v10nr_clust-nr.hgnc-m0.001-o0.0.tbl"
elif [ "$SPECIES" == "2" ]; then
    echo "正在为 Mouse 设置 PySCENIC 路径..."
    TFS="/BioData/08.ref/pyscenic/allTFS_mm.txt"
    DB_DIR="/BioData/08.ref/pyscenic/mm/mm10_10kbp_up_10kbp_down_full_tx_v10_clust.genes_vs_motifs.rankings.feather"
    MOTIFS="/BioData/08.ref/pyscenic/motifs-v10nr_clust-nr.mgi-m0.001-o0.0.tbl"
fi

# 1. GRN 推断
pyscenic grn \
    --num_workers $THREADS \
    --method grnboost2 \
    -o adjacencies.tsv \
    $INPUT \
    $TFS

# # 2. 调控模块识别
pyscenic ctx \
    adjacencies.tsv \
    $DB_DIR \
    --annotations_fname $MOTIFS \
    --expression_mtx_fname $INPUT \
    --output regulons.csv \
    --num_workers $THREADS

# 3. AUCell 评分
pyscenic aucell \
    $INPUT \
    regulons.csv \
    -o auc_mtx.loom \
    --num_workers $THREADS


# 记录程序结束时间
end_time=$(date +"%Y-%m-%d %H:%M:%S")
end_timestamp=$(date +%s)

# 计算运行时间
total_seconds=$((end_timestamp - start_timestamp))
hours=$((total_seconds / 3600))
minutes=$(((total_seconds % 3600) / 60))
seconds=$((total_seconds % 60))

echo "======================================"
echo "程序结束时间: $end_time"
echo "总运行时间: ${hours}小时 ${minutes}分钟 ${seconds}秒"

conda activate email
python /BioData/09.script/send_email_function.py "$abs_log"

```


## 自动分析脚本

